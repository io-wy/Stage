{
  "name": "stage-web-console",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:web": "tsc -p tsconfig.web.json",
    "check:web": "tsc -p tsconfig.web.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
